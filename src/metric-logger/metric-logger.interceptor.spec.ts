import { MetricLoggerInterceptor } from './metric-logger.interceptor';

describe('MetricLoggerInterceptor', () => {
  it('should be defined', () => {
    expect(new MetricLoggerInterceptor()).toBeDefined();
  });
});
